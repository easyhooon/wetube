extends layouts/main
//- layout main의 전체 코드를 복사/붙여넣기 한다는 뜻
//- 전체를 복사했기 때문에  그 위에 덮어쓰기(overwrite) 할 수 있음 
//- 이 기능 덕분에 수많은 html 중복 코드를 쓰지 않을 수 있음! 
include mixins/videoBlock

block content
    .home-videos
        each item in videos
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            //- h1= video.title
            //- p= video.description
        
//- pug -> express에서 View를 다루는 방식 중 하나, express로 HTML을 보여줄 수 있음, res.send 대신에 실제 html을 전달 
//- p Hello!!
//- <p> Hello!!</p>로 pug가 자동 변환 해줌 (일반적인 html코드로 변환해준다는 의미 )
//- home controller에서 home 화면에 videos 배열을 전달

